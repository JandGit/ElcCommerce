package com.android.tkengine.elccommerce.UI;import android.os.Build;import android.os.Bundle;import android.support.annotation.Nullable;import android.support.v4.view.ViewPager;import android.support.v7.app.AppCompatActivity;import android.support.v7.widget.LinearLayoutManager;import android.support.v7.widget.RecyclerView;import android.view.WindowManager;import android.widget.ImageView;import com.android.tkengine.elccommerce.R;import com.android.tkengine.elccommerce.beans.DisItemBean;import com.android.tkengine.elccommerce.model.ElcModel;import com.android.tkengine.elccommerce.presenter.DisplayPresenter;import java.util.List;/** * Created by FangYu on 2016/8/9. */public class DisplayActivity extends AppCompatActivity {    ViewPager goodsPager;    DisplayPresenter presenter;    RecyclerView goodsContent;    DisplayPresenter.HomepageAdapter myRecycleViewAdapter;    private ImageView titleBack,titleMessage;    List<ImageView> mList;    int[] bannerImages = {R.mipmap.advertise1, R.mipmap.advertise2, R.mipmap.advertise3, R.mipmap.advertise4 };    @Override    protected void onCreate(@Nullable Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_display);        //实现沉浸式状态栏        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.KITKAT){            getWindow().addFlags(WindowManager.LayoutParams.FLAG_TRANSLUCENT_STATUS);        }        initView();    }    private void initView() {        titleBack = (ImageView) findViewById(R.id.title_back);        goodsPager = (ViewPager) findViewById(R.id.goods_pager);        goodsContent = (RecyclerView) findViewById(R.id.goods_content);        ElcModel mModel= new ElcModel(DisplayActivity.this);        final List<DisItemBean> data = mModel.getData(0, 10);        presenter = new DisplayPresenter();        presenter.clickBack(DisplayActivity.this, titleBack);        myRecycleViewAdapter = presenter.new HomepageAdapter(data, DisplayActivity.this);        LinearLayoutManager linearLayoutManager = new LinearLayoutManager(this,LinearLayoutManager.VERTICAL, false);        goodsContent.setAdapter(myRecycleViewAdapter);        goodsContent.setLayoutManager(linearLayoutManager);//    }}